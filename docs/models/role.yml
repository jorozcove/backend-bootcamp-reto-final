definitions:
  Role:
    type: object
    properties:
      id:
        type: integer
      name:
        type: string
      state:
        type: boolean
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time

components:
  schemas:
    Role:
      type: object
      properties:
        name:
          type: string
          description: Nombre del rol
        state:
          type: boolean
          description: Estado del rol
      required:
        - name
        - state
      example:
        name: "string"
        state: true